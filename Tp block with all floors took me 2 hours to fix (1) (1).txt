--[[
    ╔═══════════════════════════════════════════════════════════════════╗
    ║   WSA TP & BLOCK + 3rd floor by Silent Nigger                                    ║
    ╚═══════════════════════════════════════════════════════════════════╝
--]]

--// SERVICES
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local StarterGui = game:GetService("StarterGui")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")

local player = Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")
local char = player.Character or player.CharacterAdded:Wait()
local humanoid = char:WaitForChild("Humanoid")
local hrp = char:WaitForChild("HumanoidRootPart")

local REQUIRED_TOOL = "Flying Carpet"
local teleportDebounce = false
local currentMarker

--// BLOCK VARIABLES
local BlockEnabled = true
local BlockDelay = 0.014

--// EQUIP
local function equipFlyingCarpet()
    local tool = backpack:FindFirstChild(REQUIRED_TOOL) or char:FindFirstChild(REQUIRED_TOOL)
    if tool then
        humanoid:EquipTool(tool)
        return true
    end
    return false
end

--// BLOCK LOGIC (WITH CLICK AFTER DELAY)
local function clickConfirm()
    local vp = Workspace.CurrentCamera.ViewportSize
    local x = vp.X / 2
    local y = (vp.Y / 2) + 80

    VirtualInputManager:SendMouseMoveEvent(x, y, game)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
end

local function openBlockMenu(plr)
    if not BlockEnabled then return end
    pcall(function()
        StarterGui:SetCore("PromptBlockPlayer", plr)
    end)
    task.delay(BlockDelay, clickConfirm)
end

local function blockAllPlayers()
    if not BlockEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player then
            openBlockMenu(plr)
            task.wait(0.2)
        end
    end
end

--// COORDS (UPDATED WITH NEW 18+ POSITIONS)
local thirdPositions = {
    -- NORMAL (1-10)
    CFrame.new(-332.21, -5.40, 21.01),
    CFrame.new(-323.86, -5.40, 21.26),
    CFrame.new(-316.64, -5.15, 20.55),
    CFrame.new(-309.07, -5.40, 20.92),
    CFrame.new(-301.85, -5.15, 19.76),
    CFrame.new(-301.02, -5.20, -6.95),
    CFrame.new(-308.93, -5.15, -6.86),
    CFrame.new(-316.27, -5.15, -7.10),
    CFrame.new(-324.04, -5.15, -6.99),
    CFrame.new(-331.30, -5.02, -9.93),

    -- SPECIAL (11-18)
    CFrame.new(-315.28, 12.80, 20.15),
    CFrame.new(-323.82, 12.60, 21.80),
    CFrame.new(-331.14, 12.60, 21.37),
    CFrame.new(-331.33, 12.70, -7.48),
    CFrame.new(-323.89, 12.85, -7.03),
    CFrame.new(-316.67, 12.60, -7.57),
    CFrame.new(-308.22, 12.65, -7.53),
    CFrame.new(-300.91, 12.60, -7.98),

    -- NEW 18+ POSITIONS (19-26)
    CFrame.new(-298.89, 31.35, -10.77),  -- 19
    CFrame.new(-306.37, 31.15, -9.62),  -- 20
    CFrame.new(-314.19, 31.20, -9.81),  -- 21
    CFrame.new(-321.49, 31.35, -10.44), -- 22
    CFrame.new(-329.26, 31.15, -9.56),  -- 23
    CFrame.new(-314.38, 31.10, 22.49),  -- 24
    CFrame.new(-321.67, 31.10, 22.40),  -- 25
    CFrame.new(-328.40, 31.10, 22.86)   -- 26
}

local currentThirdIndex = 1

--// MARKER (TEXT)
local function updateMarker()
    if currentMarker then currentMarker:Destroy() end

    local base = Instance.new("Part")
    base.Size = Vector3.new(1,0.2,1)
    base.Anchored = true
    base.CanCollide = false
    base.Material = Enum.Material.Neon
    base.Color = Color3.fromRGB(0,170,255)
    base.CFrame = thirdPositions[currentThirdIndex]
    base.Parent = Workspace

    local gui = Instance.new("BillboardGui")
    gui.Size = UDim2.new(0,100,0,20)
    gui.StudsOffset = Vector3.new(0,2,0)
    gui.AlwaysOnTop = true
    gui.Adornee = base

    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.Text = "TP "..currentThirdIndex
    txt.TextColor3 = Color3.fromRGB(0,170,255)
    txt.TextSize = 12
    txt.Font = Enum.Font.Gotham
    txt.Parent = gui

    gui.Parent = base
    currentMarker = base
end

-- Helper: apply upward velocity for a short time
local function boostUp(duration)
    local goingUp = true
    task.spawn(function()
        while goingUp do
            RunService.Heartbeat:Wait()
            if hrp then hrp.Velocity = Vector3.new(0, 100, 0) end
        end
    end)
    task.wait(duration)
    goingUp = false
end

-- Helper: jump and wait until Y >= targetHeight, then teleport instantly
local function jumpAndTeleportAtHeight(targetCFrame, targetHeight)
    -- Apply upward velocity
    local jumping = true
    task.spawn(function()
        while jumping do
            RunService.Heartbeat:Wait()
            if hrp then hrp.Velocity = Vector3.new(0, 100, 0) end
        end
    end)
    
    -- Wait until we reach the target height (or timeout after 2 seconds)
    local startTime = tick()
    repeat
        task.wait(0.02)
        local currentY = hrp and hrp.Position.Y or 0
        if currentY >= targetHeight then
            break
        end
        if tick() - startTime > 2 then
            break
        end
    until false
    
    jumping = false
    hrp.CFrame = targetCFrame
end

--// TELEPORT LOGIC (MODIFIED FOR 18+ WITH JUMP-AND-TELEPORT AT HEIGHT)
local function teleportAll()
    if teleportDebounce then return end
    teleportDebounce = true

    if not equipFlyingCarpet() then
        teleportDebounce = false
        return
    end

    blockAllPlayers()
    task.wait(0.001)

    if currentThirdIndex <= 10 then
        -- NORMAL TELEPORT (1-10)
        hrp.CFrame = CFrame.new(-364.62, -7.30, 113.09)
        task.wait(0.2)
        hrp.CFrame = CFrame.new(-344.83, -7.30, 24.67)
        task.wait(0.2)
        hrp.CFrame = thirdPositions[currentThirdIndex]

    elseif currentThirdIndex <= 18 then
        -- SPECIAL TELEPORT (11-18) with jump up
        hrp.CFrame = CFrame.new(-355.56, -7.30, 90.27)
        boostUp(0.1)
        task.wait(0.6)
        hrp.CFrame = CFrame.new(-299.18, 12.60, 35.95)
        task.wait(0.4)
        hrp.CFrame = CFrame.new(-302.87, 12.60, 12.20)
        task.wait(0.001)
        hrp.CFrame = CFrame.new(-305.15, 12.66, 9.82)
        task.wait(0.1)
        hrp.CFrame = thirdPositions[currentThirdIndex]

    else
        -- NEW 18+ TELEPORT (19-26) – jump naturally and teleport instantly at height
        -- First, follow the 11-18 "get up" sequence to reach the lower platform
        hrp.CFrame = CFrame.new(-355.56, -7.30, 90.27)
        boostUp(0.1)
        task.wait(0.6)
        hrp.CFrame = CFrame.new(-299.18, 12.60, 35.95)
        task.wait(0.4)
        hrp.CFrame = CFrame.new(-302.87, 12.60, 12.20)
        task.wait(0.001)
        hrp.CFrame = CFrame.new(-305.15, 12.66, 9.82)
        task.wait(0.1)

        -- Now jump upward and teleport as soon as we reach Y >= 31
        jumpAndTeleportAtHeight(thirdPositions[currentThirdIndex], 31)
    end

    teleportDebounce = false
end

--==============================================================================
-- KZR AUTO GRAB V2 INTEGRATION (FULLY BLUE THEME)
--==============================================================================

--// SYNCHRONIZER
local function getInternalTable()
    local Packages = ReplicatedStorage:FindFirstChild("Packages")
    if not Packages then return nil end
    local SynMod = Packages:FindFirstChild("Synchronizer")
    if not SynMod then return nil end
    local ok, syn = pcall(require, SynMod)
    if not ok or not syn then return nil end
    local Get = syn.Get
    if type(Get) ~= "function" then return nil end
    for i = 1, 5 do
        local s, u = pcall(getupvalue, Get, i)
        if s and type(u) == "table" then
            if u.___private or u.___channels or u.___data then return u end
            for k, v in pairs(u) do
                if (type(k) == "string" and k:match("^Plot_")) or type(v) == "table" then return u end
            end
        end
    end
    local s, e = pcall(getfenv, Get)
    if s and e and e.self then return e.self end
    return nil
end

local SyncInt = { _cache = {}, _data = nil }
task.spawn(function()
    for i = 1, 10 do
        SyncInt._data = getInternalTable()
        if SyncInt._data then break end
        task.wait(1)
    end
end)

local function stealthGet(n)
    if not n or type(n) ~= "string" then return nil end
    if SyncInt._cache[n] == false then return nil end
    if SyncInt._data then
        for _, k in ipairs({ n, "Plot_" .. n, "Plot" .. n, n .. "_Channel", "Channel_" .. n }) do
            if SyncInt._data[k] then
                SyncInt._cache[n] = SyncInt._data[k]
                return SyncInt._data[k]
            end
        end
        for k, v in pairs(SyncInt._data) do
            if type(k) == "string" and (k == n or k:find(n, 1, true)) and type(v) == "table" then
                SyncInt._cache[n] = v
                return v
            end
        end
    end
    SyncInt._cache[n] = false
    return nil
end

local function sProp(ch, p)
    if not ch or type(ch) ~= "table" then return nil end
    if ch[p] then return ch[p] end
    if type(ch.Get) == "function" then
        local ok, r = pcall(ch.Get, ch, p)
        if ok then return r end
    end
    local alts = {
        Owner = { "owner", "Owner", "plotOwner", "PlotOwner" },
        AnimalList = { "animalList", "AnimalList", "animals", "Animals", "pets" }
    }
    if alts[p] then
        for _, a in ipairs(alts[p]) do
            if ch[a] then return ch[a] end
        end
    end
    return nil
end

--// MODULES
local Datas = ReplicatedStorage:WaitForChild("Datas")
local Shared = ReplicatedStorage:WaitForChild("Shared")
local Utils = ReplicatedStorage:WaitForChild("Utils")

local AnimalsData, AnimalsShared, NumberUtils
task.spawn(function()
    for i = 1, 10 do
        local s1, d = pcall(require, Datas:WaitForChild("Animals"))
        local s2, sh = pcall(require, Shared:WaitForChild("Animals"))
        local s3, nu = pcall(require, Utils:WaitForChild("NumberUtils"))
        if s1 and d then AnimalsData = d end
        if s2 and sh then AnimalsShared = sh end
        if s3 and nu then NumberUtils = nu end
        if AnimalsData and AnimalsShared and NumberUtils then break end
        task.wait(0.5)
    end
end)

--// STATE
local allAnimals = {}
local plotChannels = {}
local lastHash = {}
local PromptCache = {}
local StealCache = {}

local autoMode = true
local selIdx = 1
local enabled = false
local rainbowClock = 0
local espObjects = {}

local TRIGGER_DIST = 10
local CHARGE_SPEED = 70
local DIP_AMOUNT = 8
local DIP_SPEED = 100
local RUSH_SPEED = 420
local STEAL_COOLDOWN = 0.4

local stealPct = 0
local stealTarget = nil
local stealPhase = "idle"
local dipTarget = 0
local lastStealTime = 0
local stealFiring = false

local G = {}

--// COULEURS (FULLY BLUE THEME)
local C = {
    bg = Color3.fromRGB(2, 2, 4),
    blue = Color3.fromRGB(0, 120, 255),
    blueLight = Color3.fromRGB(100, 180, 255),
    text = Color3.fromRGB(255, 255, 255),
    textDim = Color3.fromRGB(150, 200, 255),
    danger = Color3.fromRGB(239, 68, 68),
    off = Color3.fromRGB(25, 30, 45),
    green = Color3.fromRGB(0, 150, 255),
}

local function hsv(h, s, v)
    local i = math.floor(h * 6)
    local f = h * 6 - i
    local p, q, t = v * (1 - s), v * (1 - f * s), v * (1 - (1 - f) * s)
    i = i % 6
    local r, g, b
    if i == 0 then r, g, b = v, t, p
    elseif i == 1 then r, g, b = q, v, p
    elseif i == 2 then r, g, b = p, v, t
    elseif i == 3 then r, g, b = p, q, v
    elseif i == 4 then r, g, b = t, p, v
    else r, g, b = v, p, q
    end
    return Color3.new(r, g, b)
end

--// MY BASE
local function isMyBase(ad)
    if not ad or not ad.plot then return false end
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return false end
    local plot = plots:FindFirstChild(ad.plot)
    if not plot then return false end
    local ch = stealthGet(plot.Name)
    if ch then
        local o = sProp(ch, "Owner")
        if o then
            if typeof(o) == "Instance" and o:IsA("Player") then return o.UserId == player.UserId end
            if type(o) == "table" and o.UserId then return o.UserId == player.UserId end
            if typeof(o) == "Instance" then return o == player end
        end
    end
    local sign = plot:FindFirstChild("PlotSign")
    if sign then
        local yb = sign:FindFirstChild("YourBase")
        if yb and yb:IsA("BillboardGui") then return yb.Enabled end
    end
    return false
end

--// FIND PET MODEL / POSITION
local function findPetModel(ad)
    if not ad then return nil end
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local plot = plots:FindFirstChild(ad.plot)
    if not plot then return nil end
    local pods = plot:FindFirstChild("AnimalPodiums")
    if not pods then return nil end
    local pod = pods:FindFirstChild(ad.slot)
    if not pod then return nil end
    local base = pod:FindFirstChild("Base")
    if base then
        local spwn = base:FindFirstChild("Spawn")
        if spwn then
            for _, c in ipairs(spwn:GetChildren()) do
                if c:IsA("Model") or c:IsA("BasePart") then return c end
            end
        end
    end
    local function deepFind(parent)
        for _, c in ipairs(parent:GetChildren()) do
            if c:IsA("Model") or (c:IsA("BasePart") and c.Name ~= "Base") then return c end
            local r = deepFind(c)
            if r then return r end
        end
        return nil
    end
    return deepFind(pod)
end

local function getPetPosition(ad)
    local model = findPetModel(ad)
    if not model then return nil end
    if model:IsA("Model") then
        if model.PrimaryPart then return model.PrimaryPart.Position end
        local ok, cf = pcall(function() return model:GetBoundingBox() end)
        if ok and cf then return cf.Position end
        local part = model:FindFirstChildWhichIsA("BasePart")
        if part then return part.Position end
    elseif model:IsA("BasePart") then
        return model.Position
    end
    return nil
end

local function getPlayerPos()
    local char = player.Character
    if not char then return nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    return hrp.Position
end

local function distToTarget(ad)
    local petPos = getPetPosition(ad)
    if not petPos then return math.huge end
    local plrPos = getPlayerPos()
    if not plrPos then return math.huge end
    return (petPos - plrPos).Magnitude
end

--// ESP (BLUE THEME)
local function clearESP(uid)
    if espObjects[uid] then
        local e = espObjects[uid]
        if e.hl and e.hl.Parent then e.hl:Destroy() end
        if e.bb and e.bb.Parent then e.bb:Destroy() end
        espObjects[uid] = nil
    end
end

local function clearAllESP()
    for uid in pairs(espObjects) do clearESP(uid) end
end

local function applyESP(ad)
    if not ad then return end
    clearESP(ad.uid)
    local model = findPetModel(ad)
    if not model then return end
    local hl = Instance.new("Highlight")
    hl.FillTransparency = 0.3
    hl.OutlineTransparency = 0
    hl.FillColor = Color3.fromRGB(0, 120, 255)
    hl.OutlineColor = Color3.fromRGB(0, 100, 200)
    hl.Adornee = model
    hl.Parent = workspace
    local anchor = model:IsA("Model") and (model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")) or model
    local bb = Instance.new("BillboardGui")
    bb.AlwaysOnTop = true
    bb.Size = UDim2.new(0, 140, 0, 28)
    bb.StudsOffset = Vector3.new(0, 3.5, 0)
    bb.Adornee = anchor
    bb.Parent = workspace
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 9
    lbl.TextColor3 = Color3.fromRGB(100, 180, 255)
    lbl.TextStrokeTransparency = 0
    lbl.TextStrokeColor3 = Color3.new(0, 0, 0)
    lbl.Text = (ad.name or "?") .. "\n" .. (ad.genText or "")
    lbl.Parent = bb
    espObjects[ad.uid] = { hl = hl, bb = bb, lbl = lbl }
end

RunService.Heartbeat:Connect(function(dt)
    rainbowClock = rainbowClock + dt
    for uid, e in pairs(espObjects) do
        if e.hl and e.hl.Parent then
            e.hl.FillColor = Color3.fromRGB(0, 120, 255)
            e.hl.OutlineColor = Color3.fromRGB(0, 100, 200)
        else
            espObjects[uid] = nil
        end
        if e.lbl then e.lbl.TextColor3 = Color3.fromRGB(100, 180, 255) end
    end
end)

local function refreshESP()
    clearAllESP()
    if not enabled then return end
    if autoMode then
        for _, pet in ipairs(allAnimals) do
            if not isMyBase(pet) then applyESP(pet) end
        end
    else
        local pet = allAnimals[selIdx]
        if pet and not isMyBase(pet) then applyESP(pet) end
    end
end

--// FIND PROMPT
local function findPrompt(ad)
    if not ad then return nil end
    local c = PromptCache[ad.uid]
    if c and c.Parent then return c end
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local plot = plots:FindFirstChild(ad.plot)
    if not plot then return nil end
    local pods = plot:FindFirstChild("AnimalPodiums")
    if not pods then return nil end
    local pod = pods:FindFirstChild(ad.slot)
    if not pod then return nil end
    local base = pod:FindFirstChild("Base")
    if not base then return nil end
    local spwn = base:FindFirstChild("Spawn")
    if not spwn then return nil end
    local att = spwn:FindFirstChild("PromptAttachment")
    if not att then return nil end
    for _, p in ipairs(att:GetChildren()) do
        if p:IsA("ProximityPrompt") then
            PromptCache[ad.uid] = p
            return p
        end
    end
    return nil
end

--// STEAL CALLBACKS
local function buildCallbacks(prompt)
    if StealCache[prompt] then return end
    local data = { hold = {}, trig = {}, ready = true }
    local ok1, c1 = pcall(getconnections, prompt.PromptButtonHoldBegan)
    if ok1 and type(c1) == "table" then
        for _, c in ipairs(c1) do
            if type(c.Function) == "function" then table.insert(data.hold, c.Function) end
        end
    end
    local ok2, c2 = pcall(getconnections, prompt.Triggered)
    if ok2 and type(c2) == "table" then
        for _, c in ipairs(c2) do
            if type(c.Function) == "function" then table.insert(data.trig, c.Function) end
        end
    end
    if #data.hold > 0 or #data.trig > 0 then StealCache[prompt] = data end
end

local function runList(list)
    for _, fn in ipairs(list) do task.spawn(fn) end
end

local function fireSteal(prompt)
    local data = StealCache[prompt]
    if not data then return end
    if #data.hold > 0 then runList(data.hold) end
    task.wait(0.05)
    if #data.trig > 0 then runList(data.trig) end
end

--// PLOT SCANNER
local function getHash(al)
    if not al then return "" end
    local h = ""
    for s, d in pairs(al) do
        if type(d) == "table" then
            h = h .. tostring(s) .. tostring(d.Index) .. tostring(d.Mutation)
        end
    end
    return h
end

local function scanPlot(plot)
    pcall(function()
        local uid = plot.Name
        local ch = stealthGet(uid)
        if not ch then return end
        local al = sProp(ch, "AnimalList")
        local hash = getHash(al)
        if lastHash[uid] == hash then return end
        lastHash[uid] = hash
        for i = #allAnimals, 1, -1 do
            if allAnimals[i].plot == uid then table.remove(allAnimals, i) end
        end
        local owner = sProp(ch, "Owner")
        if not owner or not Players:FindFirstChild(owner.Name) then return end
        if owner.UserId == player.UserId then return end
        if not al then return end
        for slot, ad in pairs(al) do
            if type(ad) == "table" then
                local nm = ad.Index
                local info = AnimalsData and AnimalsData[nm]
                if not info then continue end
                local gen = AnimalsShared and AnimalsShared:GetGeneration(nm, ad.Mutation, ad.Traits, nil) or 0
                local genT = "$" .. (NumberUtils and NumberUtils:ToString(gen) or tostring(gen)) .. "/s"
                table.insert(allAnimals, {
                    name = info.DisplayName or nm,
                    genText = genT,
                    genValue = gen,
                    mutation = ad.Mutation or "None",
                    owner = owner.Name or "?",
                    plot = uid,
                    slot = tostring(slot),
                    uid = uid .. "_" .. tostring(slot),
                })
            end
        end
        table.sort(allAnimals, function(a, b) return a.genValue > b.genValue end)
    end)
end

local function setupPlot(plot)
    if plotChannels[plot.Name] then return end
    local ch
    for i = 1, 3 do
        ch = stealthGet(plot.Name)
        if ch then break end
        task.wait(0.3)
    end
    if not ch then return end
    plotChannels[plot.Name] = true
    scanPlot(plot)
    plot.DescendantAdded:Connect(function() task.wait(0.05); scanPlot(plot) end)
    plot.DescendantRemoving:Connect(function() task.wait(0.05); scanPlot(plot) end)
    task.spawn(function()
        while plot.Parent and plotChannels[plot.Name] do
            task.wait(1)
            scanPlot(plot)
        end
    end)
end

local function initScanner()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then
        for i = 1, 30 do
            plots = workspace:FindFirstChild("Plots")
            if plots then break end
            task.wait(0.5)
        end
        if not plots then return end
    end
    for _, p in ipairs(plots:GetChildren()) do task.spawn(setupPlot, p) end
    plots.ChildAdded:Connect(function(p) task.wait(0.2); task.spawn(setupPlot, p) end)
    plots.ChildRemoved:Connect(function(p)
        plotChannels[p.Name] = nil
        lastHash[p.Name] = nil
        for i = #allAnimals, 1, -1 do
            if allAnimals[i].plot == p.Name then table.remove(allAnimals, i) end
        end
    end)
end

--// STEAL SYSTEM V2
local function getTargetAnimal()
    if not enabled then return nil end
    if autoMode then
        if #allAnimals == 0 then return nil end
        local best, bestDist = nil, math.huge
        for _, pet in ipairs(allAnimals) do
            if not isMyBase(pet) then
                local d = distToTarget(pet)
                if d < bestDist then bestDist = d; best = pet end
            end
        end
        return best
    else
        local pet = allAnimals[selIdx]
        if pet and not isMyBase(pet) then return pet end
        return nil
    end
end

--==============================================================================
-- COMBINED GUI (SMALL VERSION) - FULLY BLUE THEME
--==============================================================================

local playerGui = player:WaitForChild("PlayerGui")

if playerGui:FindFirstChild("WSA_HUB_SCREEN") then playerGui.WSA_HUB_SCREEN:Destroy() end
if playerGui:FindFirstChild("InstantGrabGui") then playerGui.InstantGrabGui:Destroy() end
if playerGui:FindFirstChild("KZRAutoGrabUI") then playerGui.KZRAutoGrabUI:Destroy() end

local sg = Instance.new("ScreenGui")
sg.Name = "WSA_TP_GRAB_HUB"
sg.ResetOnSpawn = false
sg.IgnoreGuiInset = true
sg.Parent = playerGui

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 220, 0, 380)
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 25, 45)
mainFrame.BackgroundTransparency = 0.08
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = sg

local blur = Instance.new("BlurEffect")
blur.Size = 8
blur.Parent = mainFrame

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 15)
corner.Parent = mainFrame

local shadow = Instance.new("Frame")
shadow.Name = "Shadow"
shadow.Size = UDim2.new(1, 10, 1, 10)
shadow.Position = UDim2.new(0, -5, 0, -5)
shadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
shadow.BackgroundTransparency = 0.7
shadow.BorderSizePixel = 0
shadow.ZIndex = 0
shadow.Parent = mainFrame
Instance.new("UICorner", shadow).CornerRadius = UDim.new(0, 20)

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 40, 70)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 25, 50))
})
gradient.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "WSA tp block"
title.TextColor3 = Color3.fromRGB(100, 180, 255)
title.TextSize = 11
title.Font = Enum.Font.GothamBlack
title.TextXAlignment = Enum.TextXAlignment.Center
title.Parent = mainFrame

local titleGlow = Instance.new("Frame")
titleGlow.Size = UDim2.new(0.8, 0, 0, 1)
titleGlow.Position = UDim2.new(0.1, 0, 0, 28)
titleGlow.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
titleGlow.BackgroundTransparency = 0.5
titleGlow.BorderSizePixel = 0
titleGlow.Parent = mainFrame
Instance.new("UICorner", titleGlow).CornerRadius = UDim.new(0, 2)

local separator = Instance.new("Frame")
separator.Size = UDim2.new(0.9, 0, 0, 1)
separator.Position = UDim2.new(0.05, 0, 0, 35)
separator.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
separator.BackgroundTransparency = 0.7
separator.BorderSizePixel = 0
separator.Parent = mainFrame

-- SECTION 1: TELEPORT
local tpSectionTitle = Instance.new("TextLabel")
tpSectionTitle.Size = UDim2.new(1, 0, 0, 16)
tpSectionTitle.Position = UDim2.new(0, 0, 0, 40)
tpSectionTitle.BackgroundTransparency = 1
tpSectionTitle.Text = "TELEPORT"
tpSectionTitle.TextColor3 = Color3.fromRGB(150, 180, 210)
tpSectionTitle.TextSize = 9
tpSectionTitle.Font = Enum.Font.GothamBold
tpSectionTitle.TextXAlignment = Enum.TextXAlignment.Center
tpSectionTitle.Parent = mainFrame

local tpButton = Instance.new("TextButton")
tpButton.Size = UDim2.new(0.85, 0, 0, 28)
tpButton.Position = UDim2.new(0.5, 0, 0, 58)
tpButton.AnchorPoint = Vector2.new(0.5, 0.5)
tpButton.BackgroundColor3 = Color3.fromRGB(50, 120, 220)
tpButton.Text = "TP"
tpButton.TextColor3 = Color3.fromRGB(255, 255, 255)
tpButton.TextSize = 10
tpButton.Font = Enum.Font.GothamBold
tpButton.AutoButtonColor = false
tpButton.Parent = mainFrame
local tpBtnCorner = Instance.new("UICorner")
tpBtnCorner.CornerRadius = UDim.new(0, 8)
tpBtnCorner.Parent = tpButton

local nextButton = Instance.new("TextButton")
nextButton.Size = UDim2.new(0.85, 0, 0, 26)
nextButton.Position = UDim2.new(0.5, 0, 0, 88)
nextButton.AnchorPoint = Vector2.new(0.5, 0.5)
nextButton.BackgroundColor3 = Color3.fromRGB(30, 40, 60)
nextButton.Text = "NEXT TP (" .. currentThirdIndex .. "/" .. #thirdPositions .. ")"
nextButton.TextColor3 = Color3.fromRGB(100, 180, 255)
nextButton.TextSize = 9
nextButton.Font = Enum.Font.GothamBold
nextButton.AutoButtonColor = false
nextButton.Parent = mainFrame
local nextBtnCorner = Instance.new("UICorner")
nextBtnCorner.CornerRadius = UDim.new(0, 8)
nextBtnCorner.Parent = nextButton

local nextBorder = Instance.new("UIStroke")
nextBorder.Thickness = 1
nextBorder.Color = Color3.fromRGB(100, 180, 255)
nextBorder.Transparency = 0.5
nextBorder.Parent = nextButton

local tpSeparator = Instance.new("Frame")
tpSeparator.Size = UDim2.new(0.9, 0, 0, 1)
tpSeparator.Position = UDim2.new(0.05, 0, 0, 105)
tpSeparator.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
tpSeparator.BackgroundTransparency = 0.7
tpSeparator.BorderSizePixel = 0
tpSeparator.Parent = mainFrame

-- SECTION 2: BLOCK SETTINGS
local blockSectionTitle = Instance.new("TextLabel")
blockSectionTitle.Size = UDim2.new(1, 0, 0, 16)
blockSectionTitle.Position = UDim2.new(0, 0, 0, 110)
blockSectionTitle.BackgroundTransparency = 1
blockSectionTitle.Text = "BLOCK SETTINGS"
blockSectionTitle.TextColor3 = Color3.fromRGB(150, 180, 210)
blockSectionTitle.TextSize = 9
blockSectionTitle.Font = Enum.Font.GothamBold
blockSectionTitle.TextXAlignment = Enum.TextXAlignment.Center
blockSectionTitle.Parent = mainFrame

local blockToggleButton = Instance.new("TextButton")
blockToggleButton.Size = UDim2.new(0.4, 0, 0, 26)
blockToggleButton.Position = UDim2.new(0.25, 0, 0, 135)
blockToggleButton.AnchorPoint = Vector2.new(0.5, 0.5)
blockToggleButton.BackgroundColor3 = Color3.fromRGB(50, 120, 220)
blockToggleButton.Text = "BLOCK: ON"
blockToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
blockToggleButton.TextSize = 9
blockToggleButton.Font = Enum.Font.GothamBold
blockToggleButton.AutoButtonColor = false
blockToggleButton.Parent = mainFrame
local blockToggleCorner = Instance.new("UICorner")
blockToggleCorner.CornerRadius = UDim.new(0, 8)
blockToggleCorner.Parent = blockToggleButton

local delayLabel = Instance.new("TextLabel")
delayLabel.Size = UDim2.new(0.4, 0, 0, 16)
delayLabel.Position = UDim2.new(0.75, 0, 0, 126)
delayLabel.AnchorPoint = Vector2.new(0.5, 0.5)
delayLabel.BackgroundTransparency = 1
delayLabel.Text = "Delay:"
delayLabel.TextColor3 = Color3.fromRGB(150, 180, 210)
delayLabel.TextSize = 8
delayLabel.Font = Enum.Font.GothamMedium
delayLabel.Parent = mainFrame

local delayTextBox = Instance.new("TextBox")
delayTextBox.Size = UDim2.new(0.4, 0, 0, 26)
delayTextBox.Position = UDim2.new(0.75, 0, 0, 148)
delayTextBox.AnchorPoint = Vector2.new(0.5, 0.5)
delayTextBox.BackgroundColor3 = Color3.fromRGB(30, 40, 60)
delayTextBox.Text = tostring(BlockDelay)
delayTextBox.TextColor3 = Color3.fromRGB(100, 180, 255)
delayTextBox.TextSize = 9
delayTextBox.Font = Enum.Font.GothamBold
delayTextBox.PlaceholderText = "0.014"
delayTextBox.ClearTextOnFocus = false
delayTextBox.Parent = mainFrame
local delayBoxCorner = Instance.new("UICorner")
delayBoxCorner.CornerRadius = UDim.new(0, 6)
delayBoxCorner.Parent = delayTextBox

local delayBorder = Instance.new("UIStroke")
delayBorder.Thickness = 1
delayBorder.Color = Color3.fromRGB(100, 180, 255)
delayBorder.Transparency = 0.5
delayBorder.Parent = delayTextBox

local blockSeparator = Instance.new("Frame")
blockSeparator.Size = UDim2.new(0.9, 0, 0, 1)
blockSeparator.Position = UDim2.new(0.05, 0, 0, 162)
blockSeparator.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
blockSeparator.BackgroundTransparency = 0.7
blockSeparator.BorderSizePixel = 0
blockSeparator.Parent = mainFrame

-- SECTION 3: AUTO GRAB
local grabSectionTitle = Instance.new("TextLabel")
grabSectionTitle.Size = UDim2.new(1, 0, 0, 16)
grabSectionTitle.Position = UDim2.new(0, 0, 0, 168)
grabSectionTitle.BackgroundTransparency = 1
grabSectionTitle.Text = "AUTO GRAB"
grabSectionTitle.TextColor3 = Color3.fromRGB(100, 180, 255)
grabSectionTitle.TextSize = 9
grabSectionTitle.Font = Enum.Font.GothamBold
grabSectionTitle.TextXAlignment = Enum.TextXAlignment.Center
grabSectionTitle.Parent = mainFrame

local grabToggleRow = Instance.new("Frame")
grabToggleRow.Size = UDim2.new(1, -12, 0, 36)
grabToggleRow.Position = UDim2.new(0, 6, 0, 186)
grabToggleRow.BackgroundTransparency = 1
grabToggleRow.Parent = mainFrame

local statusLbl = Instance.new("TextLabel", grabToggleRow)
statusLbl.Size = UDim2.new(0.65, 0, 1, 0)
statusLbl.Position = UDim2.new(0, 4, 0, 0)
statusLbl.BackgroundTransparency = 1
statusLbl.Text = "DEACTIVATE"
statusLbl.TextColor3 = C.danger
statusLbl.Font = Enum.Font.GothamBold
statusLbl.TextSize = 11
statusLbl.TextXAlignment = Enum.TextXAlignment.Left
statusLbl.ZIndex = 4
G.statusLbl = statusLbl

local grabToggleBg = Instance.new("Frame", grabToggleRow)
grabToggleBg.Size = UDim2.new(0, 40, 0, 20)
grabToggleBg.Position = UDim2.new(1, -46, 0.5, -10)
grabToggleBg.BackgroundColor3 = C.off
grabToggleBg.ZIndex = 4
Instance.new("UICorner", grabToggleBg).CornerRadius = UDim.new(1, 0)
G.toggleBg = grabToggleBg

local grabToggleCircle = Instance.new("Frame", grabToggleBg)
grabToggleCircle.Size = UDim2.new(0, 16, 0, 16)
grabToggleCircle.Position = UDim2.new(0, 2, 0.5, -8)
grabToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
grabToggleCircle.ZIndex = 5
Instance.new("UICorner", grabToggleCircle).CornerRadius = UDim.new(1, 0)
G.toggleCircle = grabToggleCircle

local grabToggleClickBtn = Instance.new("TextButton", grabToggleRow)
grabToggleClickBtn.Size = UDim2.new(1, 0, 1, 0)
grabToggleClickBtn.BackgroundTransparency = 1
grabToggleClickBtn.Text = ""
grabToggleClickBtn.ZIndex = 7

local pctLbl = Instance.new("TextLabel", grabToggleRow)
pctLbl.Size = UDim2.new(0, 50, 0, 24)
pctLbl.Position = UDim2.new(1, -100, 0, 6)
pctLbl.BackgroundTransparency = 1
pctLbl.Text = "0%"
pctLbl.Font = Enum.Font.GothamBold
pctLbl.TextSize = 12
pctLbl.TextColor3 = Color3.fromRGB(100, 180, 255)
pctLbl.TextXAlignment = Enum.TextXAlignment.Right
G.pct = pctLbl

local barBg = Instance.new("Frame", mainFrame)
barBg.Size = UDim2.new(0.9, 0, 0, 5)
barBg.Position = UDim2.new(0.05, 0, 0, 224)
barBg.BackgroundColor3 = Color3.fromRGB(25, 30, 45)
barBg.BorderSizePixel = 0
Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)
barBg.Parent = mainFrame

local barFill = Instance.new("Frame", barBg)
barFill.Size = UDim2.new(0, 0, 1, 0)
barFill.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
barFill.BorderSizePixel = 0
Instance.new("UICorner", barFill).CornerRadius = UDim.new(1, 0)

local barGrad = Instance.new("UIGradient", barFill)
barGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 100, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 180, 255)),
}
G.bar = barFill

local selRow = Instance.new("Frame", mainFrame)
selRow.Size = UDim2.new(1, -12, 0, 34)
selRow.Position = UDim2.new(0, 6, 0, 234)
selRow.BackgroundTransparency = 1

local selBg = Instance.new("Frame", selRow)
selBg.Size = UDim2.new(1, 0, 0.8, 0)
selBg.Position = UDim2.new(0, 0, 0.1, 0)
selBg.BackgroundColor3 = Color3.fromRGB(18, 25, 40)
selBg.BorderSizePixel = 0
Instance.new("UICorner", selBg).CornerRadius = UDim.new(0, 6)

local leftBtn = Instance.new("TextButton", selBg)
leftBtn.Size = UDim2.new(0, 30, 1, 0)
leftBtn.BackgroundTransparency = 1
leftBtn.Text = "<"
leftBtn.Font = Enum.Font.GothamBold
leftBtn.TextSize = 16
leftBtn.TextColor3 = Color3.fromRGB(100, 180, 255)
leftBtn.AutoButtonColor = false

local rightBtn = Instance.new("TextButton", selBg)
rightBtn.Size = UDim2.new(0, 30, 1, 0)
rightBtn.Position = UDim2.new(1, -30, 0, 0)
rightBtn.BackgroundTransparency = 1
rightBtn.Text = ">"
rightBtn.Font = Enum.Font.GothamBold
rightBtn.TextSize = 16
rightBtn.TextColor3 = Color3.fromRGB(100, 180, 255)
rightBtn.AutoButtonColor = false

local petLbl = Instance.new("TextLabel", selBg)
petLbl.Size = UDim2.new(1, -64, 1, 0)
petLbl.Position = UDim2.new(0, 32, 0, 0)
petLbl.BackgroundTransparency = 1
petLbl.Text = "AUTO"
petLbl.Font = Enum.Font.GothamBold
petLbl.TextSize = 11
petLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
petLbl.TextTruncate = Enum.TextTruncate.AtEnd
G.petLbl = petLbl

local grabSeparator = Instance.new("Frame")
grabSeparator.Size = UDim2.new(0.9, 0, 0, 1)
grabSeparator.Position = UDim2.new(0.05, 0, 0, 272)
grabSeparator.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
grabSeparator.BackgroundTransparency = 0.7
grabSeparator.BorderSizePixel = 0
grabSeparator.Parent = mainFrame

-- SECTION 4: STATUS
local statusSectionTitle = Instance.new("TextLabel")
statusSectionTitle.Size = UDim2.new(1, 0, 0, 14)
statusSectionTitle.Position = UDim2.new(0, 0, 0, 278)
statusSectionTitle.BackgroundTransparency = 1
statusSectionTitle.Text = "STATUS"
statusSectionTitle.TextColor3 = Color3.fromRGB(100, 180, 255)
statusSectionTitle.TextSize = 9
statusSectionTitle.Font = Enum.Font.GothamBold
statusSectionTitle.TextXAlignment = Enum.TextXAlignment.Center
statusSectionTitle.Parent = mainFrame

local statusInfo = Instance.new("TextLabel")
statusInfo.Size = UDim2.new(0.9, 0, 0, 32)
statusInfo.Position = UDim2.new(0.05, 0, 0, 296)
statusInfo.BackgroundTransparency = 1
statusInfo.Text = "Ready"
statusInfo.TextColor3 = Color3.fromRGB(150, 200, 255)
statusInfo.TextSize = 8
statusInfo.Font = Enum.Font.Gotham
statusInfo.TextWrapped = true
statusInfo.Parent = mainFrame

local function createCornerAccent(position, rotation)
    local accent = Instance.new("Frame")
    accent.Size = UDim2.new(0, 12, 0, 1.5)
    accent.Position = position
    accent.AnchorPoint = Vector2.new(0.5, 0.5)
    accent.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
    accent.BackgroundTransparency = 0.6
    accent.BorderSizePixel = 0
    accent.Rotation = rotation
    accent.Parent = mainFrame
    Instance.new("UICorner", accent).CornerRadius = UDim.new(0, 1)
    return accent
end

createCornerAccent(UDim2.new(0, 10, 0, 10), 45)
createCornerAccent(UDim2.new(1, -10, 0, 10), -45)
createCornerAccent(UDim2.new(0, 10, 1, -10), 135)
createCornerAccent(UDim2.new(1, -10, 1, -10), -135)

local borderGlow = Instance.new("Frame")
borderGlow.Size = UDim2.new(1, 6, 1, 6)
borderGlow.Position = UDim2.new(0, -3, 0, -3)
borderGlow.BackgroundColor3 = Color3.fromRGB(100, 180, 255)
borderGlow.BackgroundTransparency = 0.9
borderGlow.BorderSizePixel = 0
borderGlow.ZIndex = 0
borderGlow.Parent = mainFrame
Instance.new("UICorner", borderGlow).CornerRadius = UDim.new(0, 18)

task.spawn(function()
    while true do
        for i = 0, 1, 0.02 do
            borderGlow.BackgroundTransparency = 0.9 - (i * 0.1)
            task.wait(0.02)
        end
        for i = 1, 0, -0.02 do
            borderGlow.BackgroundTransparency = 0.8 - (i * 0.1)
            task.wait(0.02)
        end
    end
end)

local function playClickEffect(btn)
    local originalSize = btn.Size
    local originalColor = btn.BackgroundColor3
    TweenService:Create(btn, TweenInfo.new(0.08, Enum.EasingStyle.Quad), {
        BackgroundColor3 = Color3.fromRGB(80, 150, 255),
        Size = UDim2.new(originalSize.X.Scale, originalSize.X.Offset - 2, originalSize.Y.Scale, originalSize.Y.Offset - 2)
    }):Play()
    task.wait(0.08)
    TweenService:Create(btn, TweenInfo.new(0.12, Enum.EasingStyle.Quad), {
        BackgroundColor3 = originalColor,
        Size = originalSize
    }):Play()
end

-- BUTTON CONNECTIONS
tpButton.MouseButton1Click:Connect(function()
    task.spawn(playClickEffect, tpButton)
    teleportAll()
end)

nextButton.MouseButton1Click:Connect(function()
    task.spawn(playClickEffect, nextButton)
    currentThirdIndex = currentThirdIndex + 1
    if currentThirdIndex > #thirdPositions then currentThirdIndex = 1 end
    nextButton.Text = "NEXT TP (" .. currentThirdIndex .. "/" .. #thirdPositions .. ")"
    updateMarker()
end)

blockToggleButton.MouseButton1Click:Connect(function()
    BlockEnabled = not BlockEnabled
    task.spawn(playClickEffect, blockToggleButton)
    blockToggleButton.Text = BlockEnabled and "BLOCK: ON" or "BLOCK: OFF"
    if BlockEnabled then
        blockToggleButton.BackgroundColor3 = Color3.fromRGB(50, 120, 220)
        blockToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        blockToggleButton.BackgroundColor3 = Color3.fromRGB(30, 40, 60)
        blockToggleButton.TextColor3 = Color3.fromRGB(100, 180, 255)
    end
end)

delayTextBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        local inputValue = delayTextBox.Text:gsub(",", ".")
        local newDelay = tonumber(inputValue)
        if newDelay and newDelay > 0 then
            BlockDelay = newDelay
            delayTextBox.Text = tostring(BlockDelay)
        else
            delayTextBox.Text = tostring(BlockDelay)
        end
    end
end)

-- AUTO GRAB UI UPDATE
local function getPetName()
    if autoMode then return "AUTO" end
    local p = allAnimals[selIdx]
    return p and string.upper(p.name) or "NONE"
end

local function updateGrabUI()
    if G.pct then G.pct.Text = string.format("%.0f%%", stealPct) end
    if G.bar then G.bar.Size = UDim2.new(stealPct / 100, 0, 1, 0) end
    if G.toggleBg and G.toggleCircle then
        TweenService:Create(G.toggleBg, TweenInfo.new(0.25), {
            BackgroundColor3 = enabled and C.green or C.off
        }):Play()
        TweenService:Create(G.toggleCircle, TweenInfo.new(0.25, Enum.EasingStyle.Back), {
            Position = enabled and UDim2.new(1, -22, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
        }):Play()
    end
    if G.statusLbl then
        G.statusLbl.Text = enabled and "ACTIVÉ" or "DÉSACTIVÉ"
        G.statusLbl.TextColor3 = enabled and C.green or C.danger
    end
    if G.petLbl then G.petLbl.Text = getPetName() end
    if statusInfo then
        if enabled and stealTarget then
            statusInfo.Text = "Target: " .. (stealTarget.name or "?") .. " | Dist: " .. string.format("%.1f", distToTarget(stealTarget))
        elseif enabled then
            statusInfo.Text = "Scanning for pets..."
        else
            statusInfo.Text = "Auto Grab Disabled"
        end
    end
end

local function doToggle()
    enabled = not enabled
    if not enabled then
        clearAllESP()
        stealPct = 0
        stealTarget = nil
        stealPhase = "idle"
        dipTarget = 0
        stealFiring = false
    else
        stealPhase = "charging"
        refreshESP()
    end
    pcall(function()
        local s = Instance.new("Sound", SoundService)
        s.SoundId = "rbxassetid://6895079813"
        s.Volume = 0.4
        s:Play()
        game:GetService("Debris"):AddItem(s, 1)
    end)
    updateGrabUI()
end

grabToggleClickBtn.MouseButton1Click:Connect(doToggle)

leftBtn.MouseButton1Click:Connect(function()
    if autoMode then
        autoMode = false
        selIdx = math.max(1, #allAnimals)
    else
        selIdx = selIdx - 1
        if selIdx < 1 then
            autoMode = true
            selIdx = 1
        end
    end
    stealPct = 0
    stealTarget = nil
    stealPhase = enabled and "charging" or "idle"
    dipTarget = 0
    stealFiring = false
    updateGrabUI()
    refreshESP()
end)

rightBtn.MouseButton1Click:Connect(function()
    if autoMode then
        autoMode = false
        selIdx = 1
    else
        selIdx = selIdx + 1
        if selIdx > #allAnimals then
            autoMode = true
            selIdx = 1
        end
    end
    stealPct = 0
    stealTarget = nil
    stealPhase = enabled and "charging" or "idle"
    dipTarget = 0
    stealFiring = false
    updateGrabUI()
    refreshESP()
end)

-- AUTO GRAB HEARTBEAT LOOP
RunService.Heartbeat:Connect(function(dt)
    if not enabled then
        if stealPhase ~= "idle" then
            stealPct = 0
            stealTarget = nil
            stealPhase = "idle"
            dipTarget = 0
            stealFiring = false
            updateGrabUI()
        end
        return
    end

    if stealFiring then return end

    local target = getTargetAnimal()

    if target ~= stealTarget then
        stealTarget = target
        if not target then
            if stealPhase == "dipping" or stealPhase == "rushing" then
                stealPct = 98
                stealPhase = "holding"
            elseif stealPhase == "idle" then
                stealPhase = "charging"
            end
        else
            if stealPhase == "idle" then
                stealPhase = "charging"
            elseif stealPhase == "holding" or stealPhase == "dipping" or stealPhase == "rushing" then
                stealPct = 98
                stealPhase = "holding"
                dipTarget = 0
            end
        end
    end

    if not stealTarget then
        updateGrabUI()
        return
    end

    local dist = distToTarget(stealTarget)

    if stealPhase == "charging" then
        stealPct = stealPct + CHARGE_SPEED * dt
        if stealPct >= 99 then
            stealPct = 99
            stealPhase = "holding"
        end
    elseif stealPhase == "holding" then
        stealPct = 99
        if dist <= TRIGGER_DIST then
            dipTarget = 99 - DIP_AMOUNT - math.random(0, 3)
            stealPhase = "dipping"
        end
    elseif stealPhase == "dipping" then
        stealPct = stealPct - DIP_SPEED * dt
        if stealPct <= dipTarget then
            stealPct = dipTarget
            stealPhase = "rushing"
        end
    elseif stealPhase == "rushing" then
        stealPct = stealPct + RUSH_SPEED * dt
        if stealPct >= 100 then
            stealPct = 100
            updateGrabUI()
            local now = tick()
            if now - lastStealTime >= STEAL_COOLDOWN then
                lastStealTime = now
                stealFiring = true
                task.spawn(function()
                    local prompt = PromptCache[stealTarget and stealTarget.uid or ""] or findPrompt(stealTarget)
                    if prompt then
                        buildCallbacks(prompt)
                        if StealCache[prompt] and StealCache[prompt].ready then
                            fireSteal(prompt)
                        end
                    end
                    task.wait(0.25)
                    stealPct = 99
                    stealPhase = "holding"
                    dipTarget = 0
                    stealFiring = false
                    refreshESP()
                    updateGrabUI()
                end)
            else
                stealPct = 99
                stealPhase = "holding"
                dipTarget = 0
            end
            return
        end
    end

    updateGrabUI()
end)

task.spawn(function()
    while true do
        task.wait(3)
        if enabled then refreshESP() end
    end
end)

-- DRAG FUNCTIONALITY
local dragging, dragStart, startPos
mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType.Name:find("Mouse") or input.UserInputType.Name:find("Touch") then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

UserInputService.InputEnded:Connect(function()
    dragging = false
end)

-- INITIALIZATION
updateMarker()
updateGrabUI()

task.spawn(function()
    while not AnimalsData or not AnimalsShared or not NumberUtils do
        task.wait(0.5)
    end
    task.wait(1.5)
    initScanner()
    task.wait(1)
    updateGrabUI()
end)